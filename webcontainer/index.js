// import express from "express";
// import cors from "cors";

// const app = express();
// const port = 3111;

// // Middleware
// app.use(express.json());
// app.use(express.urlencoded({ extended: true }));

// // Allow all origins — this effectively removes CORS restrictions
// app.use(cors({ origin: "*" }));

// // Routes
// app.get("/", (req, res) => {
//   res.send("Welcome to a WebContainers app hey what are you doing! 🥳");
// });

// // Start server
// app.listen(port, () => {
//   console.log(`App is live at http://localhost:${port}`);
// });
